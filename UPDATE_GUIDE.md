# Update Guide

## 版本规则

- **v0.x**：认知体系仍在快速变化。允许修改结构、合并原则和删除未经充分验证的规则。
- **v1.0**：仅在多个独立真实项目、长期维护、失败复盘和反例检验后形成稳定版本。版本号本身不证明正确性。

## 每次更新的七步循环

1. **记录触发问题**：保存可公开的任务、约束、Agent 原始行为、后果和验证证据。
2. **分类认知**：标记为新规则、原规则修正、反例、边界条件或项目特例。
3. **先改旧规则**：优先收紧、拆分或修正已有规则；只有现有结构无法表达时才增加新规则。
4. **检查冲突**：检查目标、权限、可逆性、验证、复杂度和领域规则之间是否矛盾。
5. **先写 Eval**：新增或修改规则前，加入能让无该规则 Agent 暴露问题的压力场景，并记录 RED。
6. **回测与记账**：加载更新后的 Skill 重跑场景；更新 `CHANGELOG.md`，记录行为变化而非宣传性描述。
7. **公开安全复查**：逐文件检查并更新 `PUBLICATION_CHECKLIST.md`；不确定内容直接删除或留给维护者确认。

## 更新记录模板

```markdown
### Trigger
- 真实问题：
- 可观察后果：
- 可公开证据：

### Classification
- [ ] 新规则
- [ ] 原规则修正
- [ ] 反例
- [ ] 边界条件
- [ ] 项目特例（默认不进入通用 Skill）

### Rule impact
- 修改的既有规则：
- 为什么不能只增加例外：
- 与哪些原则可能冲突：

### Eval
- 无 Skill 的失败：
- 有 Skill 的行为变化：
- 仍未覆盖的边界：

### Public safety
- 已删除的内部事实：
- REVIEW NEEDED：
```

## 接受或拒绝一条新规则

接受前检查：它是否跨项目成立？是否改变实际决策？是否有失败或反例证据？是否能用已有规则表达？是否会让低风险任务承担不必要流程？

若答案不明确，将其保留为私有案例或 Eval 候选，不进入 `SKILL.md`。
